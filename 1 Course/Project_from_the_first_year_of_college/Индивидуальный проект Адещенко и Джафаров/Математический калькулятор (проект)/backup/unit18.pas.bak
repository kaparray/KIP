unit Unit18;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils, FileUtil, Forms, Controls, Graphics, Dialogs, ExtCtrls,
  ComCtrls;

type

  { TForm18 }

  TForm18 = class(TForm)
    Panel1: TPanel;
    ProgressBar1: TProgressBar;
    procedure FormCreate(Sender: TObject);
    procedure Panel1Click(Sender: TObject);
  private
    { private declarations }
  public
    { public declarations }
  end;

var
  Form18: TForm18;

implementation

{$R *.lfm}

{ TForm18 }

procedure TForm18.FormCreate(Sender: TObject);
begin

end;

procedure TForm18.Panel1Click(Sender: TObject);
begin

end;

end.

